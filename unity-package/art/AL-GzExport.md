# 地形导出

- 菜单入口：`Box3/地形导出`

## 导出步骤

1. 指定 **`Export Root`**；
2. 选择导出 `.gz` 文件的保存路径；
3. 点击 **导出** 按钮，等待进度完成。

## 导出前准备

- 确认选定的 **Root 下仅包含需要导出的方块**；
- 无法导出 Chunk；

## 导出后校验

- 检查导出文件是否成功生成（无错误提示，路径正确）；
- 建议再使用导入工具导入一次该 `.gz`，验证导出结果与原场景一致。
